{
  "name": "transport-pulse-spot-news-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:globe": "esbuild spot_news_globe.js --bundle --minify --format=iife --outfile=spot_news_globe.bundle.js"
  },
  "dependencies": {
    "three": "0.180.0",
    "lucide": "1.8.0"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
